# Flutter / Dart build artifacts and generated platform scaffolding.
# Only lib/, pubspec.yaml, analysis_options.yaml, and this README are the
# hand-written spike; everything below is regenerated by `flutter create .`.
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
build/
.packages
pubspec.lock

# Generated platform folders (recreate with `flutter create .`)
android/
ios/
linux/
macos/
windows/
web/
